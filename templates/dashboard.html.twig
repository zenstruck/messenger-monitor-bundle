<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>{% block title %}Messenger Monitor{% endblock %}</title>
    <link rel="icon" href="data:image/svg+xml,{{ include('@ZenstruckMessengerMonitor/icon.svg.twig')|url_encode }}">

    {% block stylesheets %}
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-9ndCyUaIbzAi2FUVXJi0CjmCapSmO7SnpJef0486qhLnuZ2cdeRhO02iuK6FUUVM" crossorigin="anonymous">

        <style>
            abbr[title] {
                text-decoration: none;
                border-bottom: 1px dotted;
            }

            .card-alert {
                border: 0;
                border-radius: 0;
            }

            .card-table {
                margin-bottom: 0;
            }
            .card-table tbody tr:last-child td, .card-table tbody tr:last-child th {
                border-bottom: 0;
            }
            .card-table tbody tr:last-child td:first-child, .card-table tbody tr:last-child th:first-child {
                border-bottom-left-radius: var(--bs-card-border-radius);
            }
            .card-table tbody tr:last-child td:last-child {
                border-bottom-right-radius: var(--bs-card-border-radius);
            }
        </style>
    {% endblock %}

    {% block javascripts %}
        <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.11.8/dist/umd/popper.min.js" integrity="sha384-I7E8VVD/ismYTF4hNIPjVp/Zjvgyol6VFvRkX/vR+Vc4jQkC+hVqc2pM8ODewa9r" crossorigin="anonymous"></script>
        <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.1/dist/js/bootstrap.min.js" integrity="sha384-Rx+T1VzGupg4BHQYs2gCW9It+akI2MM/mndMCy36UVfodzcJcF0GGLxZIzObiEfa" crossorigin="anonymous"></script>
    {% endblock %}
</head>
<body>
{% block navbar %}
    <nav class="navbar navbar-expand-lg bg-body-tertiary">
        <div class="container-fluid">
            <span class="navbar-brand mb-0 h1">
                {{ include('@ZenstruckMessengerMonitor/icon.svg.twig', {size: 24, class: 'd-inline-block align-text-top'}) }}
                {{ block('title') }}
            </span>
            {% block navbar_links %}{% endblock %}
        </div>
    </nav>
{% endblock %}
{% block header %}{% endblock %}
{% block breadcrumbs %}{% endblock %}
{% block body %}
    <div class="container-fluid mt-3">
        <div class="row">
            <div class="col-md-4 col-lg-3">
                <div class="card position-relative mb-3">
                    {% if workers.running %}
                        <span class="position-absolute top-0 start-100 translate-middle badge rounded-pill bg-success">
                            {{ workers.count }} <span class="visually-hidden">workers running</span>
                        </span>
                    {% else %}
                        <span class="position-absolute top-0 start-100 translate-middle badge rounded-pill bg-danger">
                            0 <span class="visually-hidden">workers running</span>
                        </span>
                    {% endif %}

                    <div class="card-header text-center">
                        Worker Status
                    </div>

                    {% if workers.running %}
                        <div class="table-responsive">
                            <table class="table card-table table-striped align-text-top text-nowrap">
                                <thead>
                                <tr>
                                    <th>&nbsp;</th>
                                    <th class="text-center">Status</th>
                                    <th>Transports</th>
                                    <th class="text-center"># Handled</th>
                                    <th class="text-center">Memory</th>
                                </tr>
                                </thead>
                                <tbody>
                                {% for worker in workers %}
                                    <tr>
                                        <td>{{ loop.index }}</td>
                                        <td class="text-center">
                                            <span class="badge align-text-top text-bg-{{ worker.isIdle ? 'success' : 'primary' }}">
                                                {{ worker.status }}
                                            </span>
                                        </td>
                                        <td>
                                            {% for transport in worker.transports %}
                                                <span class="badge align-text-top text-bg-secondary">{{ transport }}</span>
                                            {% endfor %}
                                        </td>
                                        <td class="text-center">
                                            {{ worker.messagesHandled }}
                                        </td>
                                        <td class="text-center">
                                            {{ worker.memoryUsage }}
                                        </td>
                                    </tr>
                                {% endfor %}
                                </tbody>
                            </table>
                        </div>
                    {% else %}
                        <div class="card-alert card-text alert alert-danger d-flex align-items-center" role="alert">
                            <svg fill="currentcolor" height="1em" width="1em" class="flex-shrink-0 me-2" viewBox="0 0 16 16">
                                <path d="M8.982 1.566a1.13 1.13 0 0 0-1.96 0L.165 13.233c-.457.778.091 1.767.98 1.767h13.713c.889 0 1.438-.99.98-1.767L8.982 1.566zM8 5c.535 0 .954.462.9.995l-.35 3.507a.552.552 0 0 1-1.1 0L7.1 5.995A.905.905 0 0 1 8 5zm.002 6a1 1 0 1 1 0 2 1 1 0 0 1 0-2z"/>
                            </svg>
                            <div>
                                There are no workers running.
                            </div>
                        </div>
                    {% endif %}
                </div>
                <div class="card position-relative mb-3">
                    <div class="card-header text-center">
                        Transports
                    </div>

                    {% if not transports|length %}
                        <div class="card-alert card-text alert alert-danger d-flex align-items-center" role="alert">
                            <svg fill="currentcolor" height="1em" width="1em" class="flex-shrink-0 me-2" viewBox="0 0 16 16">
                                <path d="M8.982 1.566a1.13 1.13 0 0 0-1.96 0L.165 13.233c-.457.778.091 1.767.98 1.767h13.713c.889 0 1.438-.99.98-1.767L8.982 1.566zM8 5c.535 0 .954.462.9.995l-.35 3.507a.552.552 0 0 1-1.1 0L7.1 5.995A.905.905 0 0 1 8 5zm.002 6a1 1 0 1 1 0 2 1 1 0 0 1 0-2z"/>
                            </svg>
                            <div>
                                There are transports configured.
                            </div>
                        </div>
                    {% else %}
                        <div class="table-responsive">
                            <table class="table card-table table-striped align-text-top text-nowrap">
                                <thead>
                                <tr>
                                    <th>Name</th>
                                    <th class="text-center">Queued Messages</th>
                                    <th class="text-center">Workers</th>
                                </tr>
                                </thead>
                                <tbody>
                                {% for transport in transports %}
                                    <tr>
                                        <td>
                                            <span class="badge align-text-top text-bg-secondary">{{ transport.name }}</span>
                                        </td>
                                        <td class="text-center">
                                            {% if transport.countable %}
                                                {{ transport.count }}
                                            {% else %}
                                                <em class="text-secondary">n/a</em>
                                            {% endif %}
                                        </td>
                                        <td class="text-center">
                                            <strong class="{{ transport.workers|length > 0 ? 'text-success' : 'text-danger' }}">
                                                {{ transport.workers|length }}
                                            </strong>
                                        </td>
                                    </tr>
                                {% endfor %}
                                </tbody>
                            </table>
                        </div>
                    {% endif %}
                </div>
                <div class="card position-relative mb-3">
                    <div class="card-header text-center">
                        Snapshot <em class="text-secondary">(Last 24 Hours)</em>
                    </div>
                    <div class="table-responsive">
                        <table class="table card-table table-striped align-text-top text-nowrap">
                            <tbody>
                            <tr>
                                <th class="text-end w-50">Messages Processed</th>
                                <td>
                                    {{ snapshot.totalCount }}
                                    {% if snapshot.failureCount %}
                                        <em>(<strong class="text-danger">{{ snapshot.failureCount }}</strong> failures)</em>
                                    {% endif %}
                                </td>
                            </tr>
                            <tr>
                                <th class="text-end">Fail Rate</th>
                                <td>
                                    {% set failRate = (snapshot.failRate * 100)|round %}
                                    {% if failRate < 5 %}
                                        <span class="text-success">{{ failRate }}%</span>
                                    {% elseif failRate < 10 %}
                                        <span class="text-warning">{{ failRate }}%</span>
                                    {% else %}
                                        <strong class="text-danger">{{ failRate }}%</strong>
                                    {% endif %}
                                </td>
                            </tr>
                            <tr>
                                <th class="text-end">Average Wait Time</th>
                                <td>
                                    {{ snapshot.averageWaitTime|round }}s
                                </td>
                            </tr>
                            <tr>
                                <th class="text-end">Average Handling Time</th>
                                <td>
                                    {{ snapshot.averageHandlingTime|round }}s
                                </td>
                            </tr>
                            <tr>
                                <th class="text-end">Handled Per Minute</th>
                                <td>
                                    {{ snapshot.handledPerMinute|round(2) }}
                                </td>
                            </tr>
                            <tr>
                                <th class="text-end">Handled Per Hour</th>
                                <td>
                                    {{ snapshot.handledPerHour|round(2) }}
                                </td>
                            </tr>
                            <tr>
                                <th class="text-end">Handled Per Day</th>
                                <td>
                                    {{ snapshot.handledPerDay|round(2) }}
                                </td>
                            </tr>
                            </tbody>
                        </table>
                    </div>
                </div>
            </div>
            <div class="col-md-8 col-lg-9">
                <div class="card position-relative mb-3">
                    <div class="card-header">
                        Recently Processed Messages <em class="text-secondary">(Last 5)</em>
                    </div>

                    {% set messages = messages.take(5) %}

                    {% if not messages|length %}
                        <div class="card-alert card-text alert alert-warning d-flex align-items-center" role="alert">
                            <svg fill="currentcolor" height="1em" width="1em" class="flex-shrink-0 me-2" viewBox="0 0 16 16">
                                <path d="M8 16A8 8 0 1 0 8 0a8 8 0 0 0 0 16zm.93-9.412-1 4.705c-.07.34.029.533.304.533.194 0 .487-.07.686-.246l-.088.416c-.287.346-.92.598-1.465.598-.703 0-1.002-.422-.808-1.319l.738-3.468c.064-.293.006-.399-.287-.47l-.451-.081.082-.381 2.29-.287zM8 5.5a1 1 0 1 1 0-2 1 1 0 0 1 0 2z"/>
                            </svg>
                            <div>
                                No messages.
                            </div>
                        </div>
                    {% else %}
                        <div class="table-responsive">
                            <table class="table card-table table-striped align-text-top text-nowrap">
                                <thead>
                                <tr>
                                    <th>Type</th>
                                    <th>Transport</th>
                                    <th class="text-center">Duration</th>
                                    <th class="text-center">Memory</th>
                                    <th>Tags</th>
                                    <th>Handled At</th>
                                </tr>
                                </thead>
                                <tbody>
                                {% for message in messages %}
                                    <tr>
                                        <td class="{{ message.isFailure ? 'text-danger' : 'text-success'}}">
                                            {% if message.isFailure %}
                                                <svg fill="currentcolor" height="1em" width="1em" class="me-1 align-text-bottom" viewBox="0 0 16 16">
                                                    <path d="M8.982 1.566a1.13 1.13 0 0 0-1.96 0L.165 13.233c-.457.778.091 1.767.98 1.767h13.713c.889 0 1.438-.99.98-1.767L8.982 1.566zM8 5c.535 0 .954.462.9.995l-.35 3.507a.552.552 0 0 1-1.1 0L7.1 5.995A.905.905 0 0 1 8 5zm.002 6a1 1 0 1 1 0 2 1 1 0 0 1 0-2z"/>
                                                </svg>
                                            {% else %}
                                                <svg fill="currentcolor" height="1em" width="1em" class="me-1 align-text-bottom" viewBox="0 0 16 16">
                                                    <path d="M16 8A8 8 0 1 1 0 8a8 8 0 0 1 16 0zm-3.97-3.03a.75.75 0 0 0-1.08.022L7.477 9.417 5.384 7.323a.75.75 0 0 0-1.06 1.06L6.97 11.03a.75.75 0 0 0 1.079-.02l3.992-4.99a.75.75 0 0 0-.01-1.05z"/>
                                                </svg>
                                            {% endif %}

                                            <abbr title="{{ message.type }}">{{ message.type.shortName }}</abbr>
                                        </td>
                                        <td>
                                            <span class="badge align-text-top text-bg-secondary">{{ message.transport }}</span>
                                        </td>
                                        <td class="text-center">
                                            {% if duration_format %}
                                                <abbr title="{{ message.timeToHandle }}s">{{ time_formatter.formatDuration(message.timeToHandle) }}</abbr>
                                            {% else %}
                                                {{ message.timeToHandle }}s
                                            {% endif %}
                                        </td>
                                        <td class="text-center">
                                            {{ message.memoryUsage }}
                                        </td>
                                        <td>
                                            {% for tag in message.tags %}
                                                <span class="badge align-text-top text-bg-secondary">{{ tag }}</span>
                                            {% endfor %}
                                        </td>
                                        <td>
                                            {% if time_formatter %}
                                                <abbr title="{{ message.handledAt|date() }}">{{ time_formatter.formatDiff(message.handledAt) }}</abbr>
                                            {% else %}
                                                {{ message.handledAt|date() }}
                                            {% endif %}
                                        </td>
                                    </tr>
                                {% endfor %}
                                </tbody>
                            </table>
                        </div>
                    {% endif %}
                </div>

                {% if schedules %}
                    <div class="card position-relative mb-3">
                        {% if not schedules|length %}
                            <div class="card-header">
                                Schedules
                            </div>
                            <div class="card-alert card-text alert alert-warning d-flex align-items-center" role="alert">
                                <svg fill="currentcolor" height="1em" width="1em" class="flex-shrink-0 me-2" viewBox="0 0 16 16">
                                    <path d="M8 16A8 8 0 1 0 8 0a8 8 0 0 0 0 16zm.93-9.412-1 4.705c-.07.34.029.533.304.533.194 0 .487-.07.686-.246l-.088.416c-.287.346-.92.598-1.465.598-.703 0-1.002-.422-.808-1.319l.738-3.468c.064-.293.006-.399-.287-.47l-.451-.081.082-.381 2.29-.287zM8 5.5a1 1 0 1 1 0-2 1 1 0 0 1 0 2z"/>
                                </svg>
                                <div>
                                    No schedules configured.
                                </div>
                            </div>
                        {% else %}
                            <div class="card-header">
                                <ul class="nav nav-tabs card-header-tabs">
                                    {% for schedule in schedules %}
                                        <li class="nav-item position-relative mx-2" role="presentation">
                                            {% if schedule.running %}
                                                <span class="z-2 position-absolute top-0 start-100 translate-middle badge rounded-pill bg-success">
                                                    {{ schedule.workers|length }} <span class="visually-hidden">workers running this schedule</span>
                                                </span>
                                            {% else %}
                                                <span class="z-2 position-absolute top-0 start-100 translate-middle badge rounded-pill bg-danger">
                                                    0 <span class="visually-hidden">workers running this schedule</span>
                                                </span>
                                            {% endif %}

                                            <button class="nav-link{{ loop.first ? ' active' }}" id="schedule-{{ schedule.name }}-tab" data-bs-toggle="tab" data-bs-target="#schedule-{{ schedule.name }}-tab-pane" type="button" role="tab" aria-controls="schedule-{{ schedule.name }}-tab-pane" aria-selected="{{ loop.first ? 'true' : 'false' }}">
                                                {{ schedule.name|humanize }} Schedule
                                            </button>
                                        </li>
                                    {% endfor %}
                                </ul>
                            </div>
                            <div class="tab-content">
                                {% for schedule in schedules %}
                                    <div class="tab-pane fade{{ loop.first ? ' show active' }}" id="schedule-{{ schedule.name }}-tab-pane" role="tabpanel" aria-labelledby="schedule-{{ schedule.name }}-tab" tabindex="0">
                                        <div class="table-responsive">
                                            <table class="table card-table table-striped text-nowrap align-middle">
                                                <thead>
                                                <tr>
                                                    <th>ID</th>
                                                    <th>Message</th>
                                                    <th>Trigger</th>
                                                    <th>Last Run</th>
                                                    <th class="text-center">Last Duration</th>
                                                    <th class="text-center">Last Memory</th>
                                                </tr>
                                                </thead>
                                                <tbody>
                                                {% for task in schedule %}
                                                    <tr>
                                                        <td>{{ task.id }}</td>
                                                        <td>
                                                            {% set type = task.messageType %}
                                                            {% set str = type.objectString %}

                                                            <abbr title="{{ type }}">{{ type.shortName }}</abbr>
                                                            {% if str %}
                                                                <br><small class="text-secondary">{{ str }}</small>
                                                            {% endif %}
                                                        </td>
                                                        <td>
                                                            {% set trigger = task.trigger %}

                                                            <abbr title="{{ trigger.type }}">{{ trigger.type.shortName }}</abbr>:
                                                            {{ trigger.type.objectString }}
                                                        </td>

                                                        {% set message = task.history.messages.first %}
                                                        <td{% if message %} class="{{ message.isFailure ? 'text-danger' : 'text-success'}}"{% endif %}>
                                                            {% if message and message.isFailure %}
                                                                <svg fill="currentcolor" height="1em" width="1em" class="me-1 align-text-bottom" viewBox="0 0 16 16">
                                                                    <path d="M8.982 1.566a1.13 1.13 0 0 0-1.96 0L.165 13.233c-.457.778.091 1.767.98 1.767h13.713c.889 0 1.438-.99.98-1.767L8.982 1.566zM8 5c.535 0 .954.462.9.995l-.35 3.507a.552.552 0 0 1-1.1 0L7.1 5.995A.905.905 0 0 1 8 5zm.002 6a1 1 0 1 1 0 2 1 1 0 0 1 0-2z"/>
                                                                </svg>
                                                            {% elseif message %}
                                                                <svg fill="currentcolor" height="1em" width="1em" class="me-1 align-text-bottom" viewBox="0 0 16 16">
                                                                    <path d="M16 8A8 8 0 1 1 0 8a8 8 0 0 1 16 0zm-3.97-3.03a.75.75 0 0 0-1.08.022L7.477 9.417 5.384 7.323a.75.75 0 0 0-1.06 1.06L6.97 11.03a.75.75 0 0 0 1.079-.02l3.992-4.99a.75.75 0 0 0-.01-1.05z"/>
                                                                </svg>
                                                            {% endif %}

                                                            {% if message %}
                                                                {% if time_formatter %}
                                                                    <abbr title="{{ message.handledAt|date() }}">{{ time_formatter.formatDiff(message.handledAt) }}</abbr>
                                                                {% else %}
                                                                    {{ message.handledAt|date() }}
                                                                {% endif %}
                                                            {% else %}
                                                                <em class="text-secondary">Never</em>
                                                            {% endif %}
                                                        </td>
                                                        <td class="text-center">
                                                            {% if message and duration_format %}
                                                                <abbr title="{{ message.timeToHandle }}s">{{ time_formatter.formatDuration(message.timeToHandle) }}</abbr>
                                                            {% elseif message %}
                                                                {{ message.timeToHandle }}s
                                                            {% else %}
                                                                <em class="text-secondary">-</em>
                                                            {% endif %}
                                                        </td>
                                                        <td class="text-center">
                                                            {% if message %}
                                                                {{ message.memoryUsage }}
                                                            {% else %}
                                                                <em class="text-secondary">-</em>
                                                            {% endif %}
                                                        </td>
                                                    </tr>
                                                {% endfor %}
                                                </tbody>
                                            </table>
                                        </div>
                                    </div>
                                {% endfor %}
                            </div>
                        {% endif %}
                    </div>
                {% endif %}
            </div>
        </div>
    </div>
{% endblock %}
{% block footer %}
    <footer class="p-5 bg-body-secondary text-center small">
        <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" fill="currentColor" class="me-1 text-danger" viewBox="0 0 16 16">
            <path fill-rule="evenodd" d="M8 1.314C12.438-3.248 23.534 4.735 8 15-7.534 4.736 3.562-3.248 8 1.314z"/>
        </svg>
        Enjoying this tool? Consider <a href="https://github.com/sponsors/kbond" target="_blank">sponsoring me (Kevin Bond) on GitHub</a>!
    </footer>
{% endblock %}
</body>
</html>
