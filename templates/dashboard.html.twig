{% extends '@ZenstruckMessengerMonitor/layout.html.twig' %}

{% block title %}Dashboard{% endblock %}

{% block breadcrumbs %}{% endblock %}

{% block content %}
    <div class="row">
        <div class="col-md-4 col-lg-3">
            <div class="mb-3">
                {{ include('@ZenstruckMessengerMonitor/components/workers.html.twig') }}
            </div>
            <div class="mb-3">
                {{ include('@ZenstruckMessengerMonitor/components/transports.html.twig') }}
            </div>
            {{ include('@ZenstruckMessengerMonitor/components/snapshot.html.twig', {subtitle: '(Last 24 Hours)'}) }}
        </div>
        <div class="col-md-8 col-lg-9">
            {% embed '@ZenstruckMessengerMonitor/components/messages.html.twig' with {messages: messages.take(15)} %}
                {% block card_header %}
                    <div class="d-flex justify-content-between align-items-center">
                        <span>Recently Processed Messages <em class="text-secondary">(Last 15)</em></span>
                        {% embed '@ZenstruckMessengerMonitor/components/refresh_button.html.twig' %}
                            {% block class %}{{ parent() }} btn-outline-secondary{% endblock %}
                        {% endembed %}
                    </div>
                {% endblock %}
            {% endembed %}
        </div>
    </div>
{% endblock %}
